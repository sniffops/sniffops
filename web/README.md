# SniffOps Web UI

프로덕션급 MCP 도구 보안 모니터링 대시보드

## 🎨 기술 스택

- **React 18** + **TypeScript**
- **Tailwind CSS** - 유틸리티 기반 스타일링
- **shadcn/ui** - 고품질 UI 컴포넌트
- **Vite** - 빌드 도구
- **Lucide React** - 아이콘
- **date-fns** - 날짜 포맷팅

## 🚀 빠른 시작

### 개발 모드

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:5173` 열기

### 프로덕션 빌드

```bash
npm run build
```

빌드 출력: `../internal/web/dist/` (Go embed용)

## 📁 프로젝트 구조

```
web/
├── src/
│   ├── components/
│   │   ├── ui/              # shadcn/ui 컴포넌트
│   │   ├── RiskDashboard.tsx
│   │   ├── FilterBar.tsx
│   │   ├── TraceTimeline.tsx
│   │   ├── TraceDetail.tsx
│   │   └── Stats.tsx
│   ├── lib/
│   │   ├── api.ts           # API 클라이언트
│   │   ├── mock-data.ts     # Mock 데이터
│   │   ├── types.ts         # TypeScript 타입
│   │   └── utils.ts         # 유틸리티
│   ├── App.tsx              # 메인 앱
│   ├── main.tsx             # 엔트리포인트
│   └── index.css            # 글로벌 스타일
├── index.html
├── vite.config.ts
├── tailwind.config.js
└── package.json
```

## 🎯 주요 기능

### 1. 위험도 대시보드
- 4단계 위험도 카드 (Critical/High/Medium/Low)
- 클릭 시 해당 위험도로 필터링

### 2. 필터링
- 도구별 (sniff_get, sniff_apply, etc.)
- 네임스페이스별 (production, staging, etc.)
- 위험도별
- 시간 범위 (1시간, 24시간, 7일, 30일)

### 3. 트레이스 타임라인
- 시간순 MCP 호출 기록
- 상태, 지연시간, 리소스 정보 표시
- 클릭 시 상세 모달

### 4. 트레이스 상세
- 전체 명령어 및 출력
- 위험도 평가 이유
- 토큰 사용량 및 비용 추정
- 에러 메시지 (있을 경우)

### 5. 통계 위젯
- 총 작업 수
- 총 비용
- 도구별 사용량 (막대 그래프)

## 🎨 디자인

- **다크모드 기본** (shadcn/ui 테마)
- **반응형 디자인** (모바일/태블릿/데스크톱)
- **접근성 우선** (키보드 네비게이션, ARIA 라벨)
- **일관된 스타일** (Tailwind + shadcn/ui)

## 🔧 개발

### Mock 데이터 모드

현재 `App.tsx`에서 `USE_MOCK = true`로 설정되어 있습니다.
Mock 데이터는 `src/lib/mock-data.ts`에 정의되어 있습니다.

### API 연동

백엔드 API가 준비되면:

1. `App.tsx`에서 `USE_MOCK = false` 설정
2. `src/lib/api.ts`의 함수들이 자동으로 실제 API 호출

API 엔드포인트:
- `GET /api/traces` - 트레이스 목록
- `GET /api/traces/:id` - 특정 트레이스
- `GET /api/stats` - 통계
- `GET /api/namespaces` - 네임스페이스 목록
- `GET /api/tools` - 도구 목록

## 🧪 빌드 확인

```bash
# 1. 의존성 설치
npm install

# 2. TypeScript 컴파일 + Vite 빌드
npm run build

# 3. 출력 확인
ls -lh ../internal/web/dist/
```

빌드 결과:
- `index.html` - 메인 HTML
- `assets/index-[hash].js` - 번들된 JavaScript (~212KB)
- `assets/index-[hash].css` - 번들된 CSS (~20KB)

## 📦 Go Embed 통합

빌드된 파일은 `internal/web/dist/`에 생성되어
Go의 `embed` 패키지로 바이너리에 포함됩니다:

```go
// internal/web/embed.go
package web

import "embed"

//go:embed dist/*
var DistFS embed.FS
```

## 🎯 성능 최적화

- ✅ Tree shaking (Vite)
- ✅ Code splitting
- ✅ Minification
- ✅ Gzip 압축 (~65KB JS, ~5KB CSS)
- ✅ 가벼운 dependencies (React 대신 Preact로 전환 가능)

## 🚧 향후 개선 사항

- [ ] WebSocket/SSE로 실시간 업데이트
- [ ] 차트 라이브러리 추가 (시간대별 트렌드)
- [ ] Export 기능 (CSV/JSON)
- [ ] 세션 기반 필터
- [ ] 고급 검색 (정규표현식)
- [ ] 알림 설정 UI

## 📝 라이선스

MIT

---

**Built with ❤️ by Pixel**
